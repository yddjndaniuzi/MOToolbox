# MOtoolbox 功能介绍与使用说明

MOtoolbox 是一个面向市场、发布会和媒体沟通场景的本地工具箱，主要把内容采集、整理、分析和生成放到一个应用里完成。

## 一、当前版本包含哪些功能

### 1. 发布会简报
- 支持分析本地视频、上传视频、在线视频链接和直播流。
- 自动抽取关键帧，生成截图图包。
- 可对截图进行人工筛选、隐藏和精选。
- 可基于视频转写生成简报基础稿，并继续用模型做精加工。
- 支持图文预览，并可写入飞书文档。

### 2. 副产物生成
- 可基于市场白皮书和产品简介生成卖点资产表。
- 可继续生成评测指南、新闻稿和质检清单。
- 支持飞书文档链接输入，也支持本地 Markdown 输入。

### 3. 媒体反馈
- 支持导入媒体问卷原始表单。
- 可结合参考文档，自动归纳观点、风险点和反馈结论。
- 输出结构化 Markdown 结果，方便继续修改和沉淀。

### 4. 评测视频分析
- 支持本地评测视频或在线视频链接。
- 可优先抓字幕，没有字幕时再做 ASR 转写。
- 在线字幕会优先使用人工/官方字幕，找不到时才退回自动字幕。
- Apple Silicon Mac 默认使用 MLX Whisper 的 `large-v3-turbo-q4`，走 Metal GPU；模型约 464 MB，首次使用会下载，之后复用本地缓存。实测处理发布会音频可明显快于实时播放。
- 如需切回 CPU 方案，可在启动前设置 `MOTOOLBOX_ASR_ENGINE=faster-whisper`；机器资源有限时再设置 `MOTOOLBOX_ASR_MODEL=medium` 或 `small`。已知素材语言时可设置 `MOTOOLBOX_ASR_LANGUAGE=zh` 或 `en`，避免语言误判。
- 默认使用最快的贪心解码且关闭逐词时间戳。需要逐词时间戳时可设置 `MOTOOLBOX_ASR_WORD_TIMESTAMPS=1`，但会增加耗时。MLX 失败时系统会自动改用 faster-whisper，并把失败原因和各阶段耗时写入 `transcript/quality.json`。
- 自动提炼情感倾向、正负面观点和可引用金句。

### 5. MO 速算器
- 内置屏幕和影像相关的常用参数计算。
- 适合快速核算 PPI、显示面积、焦距、像素尺寸、进光量和裁切像素等数据。

### 6. 后台控制台
- 配置模型接口和 API Key。
- 配置飞书 MCP 写入能力。
- 配置知识库路径并重建索引。

当前应用已经封装了一套可直接使用的知识库素材，内含 2023 年至 2026 年 5 月的关键发布会简报 Markdown，可直接建立索引并用于检索。
如果你已经有自己的资料体系，也可以把知识库路径指向自己的 Obsidian Vault，再按需要重建索引。

## 二、首次启动前

如果应用是本地下载或打包得到的，首次启动前需要先在终端解除 macOS 的系统安全限制：

```bash
sudo xattr -r -d com.apple.quarantine +应用路径
```

例如：

```bash
sudo xattr -r -d com.apple.quarantine /Applications/MOtoolbox.app
```

其中“`+应用路径`”请替换成你本机上的实际应用路径。

## 三、使用前需要自行准备的配置

以下配置需要用户自行准备并填写：

- 模型 API Key
- 飞书 MCP URL
- 图床 Key

对应关系可以这样理解：

- 模型 API Key：用于简报精加工、副产物生成、媒体反馈、评测视频分析等模型能力。
- 飞书 MCP URL：用于把结果写入飞书文档，或读取飞书材料。
- 图床 Key：用于导出飞书文档时上传图片，避免图片无法访问。

如果这些配置没有准备好，应用中的部分 AI 生成和飞书写入功能将无法正常使用。

## 四、基本使用流程

### 1. 打开应用
- 双击 `MOtoolbox.app` 启动。
- 应用会在本机启动一个本地服务，并自动打开浏览器页面。

### 2. 先进入后台控制台完成配置
- 配置模型信息和 API Key。
- 配置飞书 MCP URL。
- 按需要配置图床 Key。
- 如果直接使用应用内置的发布会简报库，可直接重建索引后使用。
- 如果要接入自己的资料库，再补充 Obsidian Vault 路径并重建索引。

### 3. 按模块使用

#### 发布会简报
1. 选择视频来源。
2. 填写事件名称。
3. 选择采图策略后开始采集关键帧。
   - “业务领域”可选择自动识别、通用科技、手机/消费电子、汽车、大模型/AI、机器人或芯片/半导体；人工选择优先于自动识别。
   - “输出任务”支持忠实转写、发布会速记、业务帮看和专项事实抽取。
4. 在结果页筛选截图。
5. 生成简报基础稿。
6. 按需执行模型精加工。

精加工会同时读取基础稿和带时间码的原始转写证据，不再只依赖经过裁剪的基础稿。任务目录中的 `coverage_report.json` 会记录金额、数字等事实在“原始转写 → 基础稿 → 精加工稿”之间的保留和遗漏情况；`transcript/quality.json` 会记录 ASR 模型、语言判断和低置信度片段。

多业务领域使用 `pressconf/domains/<领域>/domain.json` 配置。领域包定义识别关键词、ASR 热词、事实类型、报告章节和分析要求；新增业务领域时应增加领域包，不要继续把行业规则硬编码进通用转写代码。每次生成基础稿还会输出 `fact_ledger.json`，其中包含领域事实类型、数值、单位、原句、时间码和置信度。
7. 需要时导出到飞书文档。

选择直播流时，系统会实时采集截图并同步录制音视频；默认持续到用户点击停止，也可以填写自动停止时长作为上限。停止后，生成简报基础稿会使用这份录制文件跑 ASR。

直播分段合并结果会写入 `recording_meta.json`。如果无法形成完整录制，系统会保留全部原始分段并停止 ASR，不会再使用单个最大分段生成残缺简报。

其中“采图策略”有 3 种模式：

- `auto`：自动判断素材更像演讲型发布会还是剪辑型发布会，再自动选择更合适的采图方式。第一次使用时，优先推荐这个模式。
- `lecture`：适合“人讲 + PPT/大屏翻页”这类发布会。它更关注页面内容是否发生变化，适合保留参数页、价格页、卖点页这类相对稳定的画面。
- `program`：适合剪辑节奏快、镜头切换多、动态素材多的视频节目型发布会。它会更强调时间覆盖和关键切镜，减少漏掉动态片段的风险。

可以简单这样理解：

- 如果素材像传统发布会、现场演讲、PPT 讲解，优先选 `lecture`
- 如果素材像品牌宣传片、剪辑节目、后期包装很多的视频，优先选 `program`
- 如果暂时拿不准，先用 `auto`

#### 副产物生成
1. 输入产品名称、定位、发布时间和价格信息。
2. 提供市场白皮书与产品简介。
3. 生成卖点资产、评测指南、新闻稿和质检内容。
4. 如需调整，再补充加工要求重新生成。

#### 媒体反馈
1. 上传媒体问卷原始表单。
2. 按需补充历史反馈文档作为参考。
3. 填写会议背景和补充要求。
4. 生成结构化媒体反馈。

#### 评测视频分析
1. 选择本地视频或在线视频链接。
2. 填写产品名、媒体名和视频标题。
3. 生成逐字稿分析结果。
4. 查看情感倾向、观点摘要和金句提炼。

#### MO 速算器
- 直接输入参数即可得到计算结果。
- 适合做发布会资料核对、媒体沟通口径校验和内部快速换算。

## 五、结果文件位置

应用运行过程中产生的配置和工作文件会存放在：

```text
~/Library/Application Support/MOtoolbox/
```

这里通常会保存配置、上传文件、下载文件和生成结果。

## 六、更新日志

以下为最近几项关键改动的简要说明：

- 发布会简报的精加工链路已支持补充自定义要求，可按项目需要追加语气、结构、重点信息和删改方向，再重新生成结果。
- 新增“副产物生成”模块，可基于市场白皮书和产品简介继续生成卖点资产、评测指南、新闻稿和质检内容。
- 新增“媒体反馈”模块，可从问卷原始表单出发，整理观点、风险点和结构化反馈结果。
- 新增“评测视频分析”模块，可从本地视频或在线视频中提取字幕/转写，并输出情感倾向、正负面观点和金句。
- 发布会采图已支持更多输入来源，除本地视频外，也支持上传视频、在线视频链接和直播流。
- 发布会采图模式说明已补齐，`auto`、`lecture`、`program` 三种模式更适合不同类型的发布会素材，便于首次使用时快速选对策略。
- 知识库索引能力已补齐，应用内封装了 2023 年至 2026 年 5 月的关键发布会简报 Markdown，可直接索引使用，也支持连接到用户自己的 Obsidian Vault。
- 已补充英文发布会转写支持，处理英文素材时更稳定，适合海外发布会或英文媒体内容整理。
- 首次启动和初始化配置说明已补充，包括 macOS 安全限制解除、模型 API Key、飞书 MCP URL 和图床 Key 的准备要求。

## 七、适合谁用

这套工具更适合以下场景：

- 发布会前后做内容整理和简报沉淀
- 市场团队整理产品卖点和传播素材
- 媒体沟通会后的反馈汇总
- 评测视频舆情和观点提炼
- 手机参数相关的日常速算
